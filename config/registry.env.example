# Docker registry configuration for workBenches image push
# Copy to registry.env and customize:
#   cp registry.env.example registry.env
#
# Images are pushed as: <REGISTRY>/<image>:<username>-<date>
# Example: docker.io/opensoft/workbench-base:brett-20260227

# Docker Hub org (default)
REGISTRY=docker.io/opensoft

# Alternative registries (uncomment one):
# REGISTRY=ghcr.io/opensoft
# REGISTRY=your-acr.azurecr.io
