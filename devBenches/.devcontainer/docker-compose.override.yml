# Docker Compose Override - Personal Configuration
# =================================================
# Full host home directory is now mounted in docker-compose.yml
# This file is for any additional non-home volume overrides only

services:
  devbench: {}
