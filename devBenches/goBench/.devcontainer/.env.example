# Environment variables for the Go DevBench Monster Container
# 
# These will be automatically set by the start-monster script,
# but you can override them here if needed.
#
# To use this file:
# 1. Copy this to .env (without .example)
# 2. Modify the values as needed
# 3. Run: docker-compose up -d --build

# Your username (will be created in the container)
# USER=brett

# Your user ID (get with: id -u)
# UID=1000

# Your group ID (get with: id -g)  
# GID=1000

# Debug mode (set to true for verbose build output)
DEBUG=false

# Container name override (optional)
# COMPOSE_PROJECT_NAME=gobench
