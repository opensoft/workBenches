---
name: "Swap"
description: "/swap is the alias of /lane-swap: pause this lane for a usage reset or profile switch"
category: Lane
tags: [lane, swap, handoff, lane-collision-protocol]
---

**Invoke the `lane-swap` skill now and follow every step of it, in order, to the end.**

That is the whole of this command. `/swap` is an alias (lane-collision-protocol Amendment 11, SPEC §9): the
canonical name stays `lane-swap`, the skill is the single source of the act, and nothing here adds to,
removes from, or reorders its steps. Two copies of one procedure that must stay byte-equal is the rejected
alternative, so the steps are not restated here — read them there.
